---
interface Props {
  image: string;
  alt: string;
  eyebrow?: string;
  title: string;
  intro?: string;
  /** min-height utility class, e.g. "min-h-[70vh]" */
  minH?: string;
  /** CSS color the image bleeds into at the bottom (should match the next section's bg) */
  bleed?: string;
}
const {
  image,
  alt,
  eyebrow,
  title,
  intro,
  minH = "min-h-[70vh]",
  bleed = "var(--color-oatmeal)",
} = Astro.props;
---

<section class:list={["relative flex items-end overflow-hidden", minH]}>
  <img
    src={image}
    alt={alt}
    class="absolute inset-0 h-full w-full object-cover"
    fetchpriority="high"
  />

  <!-- readability overlay -->
  <div class="absolute inset-0 bg-gradient-to-t from-ink/80 via-ink/35 to-ink/15"></div>

  <!-- soft seam: image settles into the page below at the very bottom edge -->
  <div
    class="pointer-events-none absolute inset-x-0 bottom-0 h-20 sm:h-28"
    style={`background:linear-gradient(to bottom, rgba(0,0,0,0) 0%, ${bleed} 96%)`}
  >
  </div>

  <div class="relative mx-auto w-full max-w-7xl px-5 pb-24 pt-40 sm:px-8 sm:pb-28">
    {eyebrow && <p class="eyebrow text-white/70">{eyebrow}</p>}
    <h1 class="mt-4 max-w-3xl font-display text-4xl font-bold leading-[1.05] tracking-tight text-white sm:text-6xl">
      {title}
    </h1>
    {intro && <p class="mt-5 max-w-xl text-lg text-white/85">{intro}</p>}
    <slot />
  </div>
</section>
